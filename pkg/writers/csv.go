package writers

import (
	"os"
)

func writeToCsvFile(handle *os.File, data []map[string]interface{}) {
	// TODO: Implement this function
	/* _, err := handle.Write(data)
	if err != nil {
		common.ExitWithError(1, err)
	}
	*/
}
